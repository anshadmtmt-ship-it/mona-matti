pipeline {
    agent any

    parameters {
        choice(
            name: 'ENVIRONMENT',
            choices: ['dev', 'production'],
            description: 'Select deployment environment'
        )

        string(
            name: 'BUILD_NUMBER',
            defaultValue: '',
            description: 'CI build number to deploy (Example: 12)'
        )
    }

    environment {
        AWS_REGION = "eu-north-1"
        S3_BUCKET  = "mona-matti-kustomize-artifacts"
        KUBECONFIG = "/var/lib/jenkins/.kube/config"
    }

    stages {

        stage('Download Kustomize Artifact') {
            steps {
                withCredentials([
                    [$class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: 'aws-creds']
                ]) {

                    sh """
                        rm -rf kustomize
                        rm -f kustomize-*.tar.gz

                        aws s3 cp \
                        s3://${S3_BUCKET}/kustomize-\${BUILD_NUMBER}.tar.gz \
                        .
                    """
                }
            }
        }

        stage('Extract Artifact') {
            steps {
                sh """
                    tar -xzf kustomize-${BUILD_NUMBER}.tar.gz
                """
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                sh """
                    kubectl apply \
                    -k kustomize/overlays/${params.ENVIRONMENT}
                """
            }
        }

        stage('Verify Rollout') {
            steps {
                sh """
                    kubectl rollout status deployment/mona-matti \
                    -n ${params.ENVIRONMENT}
                """
            }
        }

    }

    post {

        success {
            echo "====================================="
            echo "Deployment Successful"
            echo "Environment : ${params.ENVIRONMENT}"
            echo "Build : ${params.BUILD_NUMBER}"
            echo "====================================="
        }

        failure {
            echo "Deployment Failed"
        }

        always {
            cleanWs()
        }
    }
}